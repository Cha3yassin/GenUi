# ============================================================
# Fberaucracy Backend — Environment Variables Template
# Copy this file to .env and fill in your real values.
# NEVER commit .env to version control.
# ============================================================

# --- Application ---
APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8000
DEBUG=true
SECRET_KEY=your-super-secret-key-change-this-in-production

# --- PostgreSQL Database ---
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=fberaucracy
POSTGRES_PASSWORD=your_postgres_password
POSTGRES_DB=fberaucracy_db

# Constructed automatically from above — or override manually:
# DATABASE_URL=postgresql+asyncpg://fberaucracy:password@localhost:5432/fberaucracy_db
# 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
# --- OpenRouter AI ---
# Create a key at https://openrouter.ai/settings/keys
OPENROUTER_API_KEY=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

OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_MODEL=google/gemini-2.5-flash
OPENROUTER_APP_TITLE= Fberaucracy
# Optional app attribution in OpenRouter rankings
# OPENROUTER_SITE_URL=http://localhost:8000

# --- Google AI Embeddings (optional, only needed for RAG ingestion/search) ---
GOOGLE_API_KEY=

# Gemini Model to use
GEMINI_MODEL=gemini-1.5-flash
EMBEDDING_MODEL=models/text-embedding-004

# --- Firebase ---
# Path to your Firebase service account JSON file
FIREBASE_SERVICE_ACCOUNT_PATH=./firebase_service_account.json

# OR paste the JSON content directly as an env var (for Docker/CI):
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"..."}

# --- ChromaDB ---
CHROMA_PERSIST_DIRECTORY=./chroma_data
CHROMA_COLLECTION_NAME=fberaucracy_procedures

# --- RAG Settings ---
RAG_CHUNK_SIZE=500
RAG_CHUNK_OVERLAP=50
RAG_TOP_K=5

# --- CORS ---
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# --- Redis + Celery ---
# Redis serves as both the cache store and the Celery broker/backend.
# Run locally via: docker-compose up redis
REDIS_URL=redis://localhost:6379/0

# --- Cache ---
# TTL for cached procedure responses (in seconds). Default: 86400 (24 hours)
CACHE_TTL_SECONDS=86400
# Set to false to disable caching (useful in dev without Redis running)
CACHE_ENABLED=true
